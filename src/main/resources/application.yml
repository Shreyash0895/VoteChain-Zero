spring:
  application:
    name: votechain-zero

  datasource:
    url: jdbc:postgresql://localhost:5432/votechain_zero
    username: votechain_user
    password: votechain_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

votechain:
  jwt:
    secret: CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_KEY_IN_PRODUCTION_ENV
    expiration-ms: 3600000
  blockchain:
    mining-difficulty: 4
    votes-per-block: 5
  otp:
    expiry-minutes: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html